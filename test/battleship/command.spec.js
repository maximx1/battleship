var Command = require("../../src/battleship/command.js");
var Tuple = require("tuple-w");

describe("Command", function() {
//	describe("object", function() {
//		it("should build an object successfully with coordinates", function() {
//			var command = new Command("1234", new Tuple(1,3), 10000);
//			expect(command.x).to.equal(1);
//			expect(command.y).to.equal(3);
//		});
//	});
	describe("#determineHit()", function() {
		it("should mark a hit if the coordinates match the passed in map", function() {
			expect(true).to.equal(true);
		});
	});
});
